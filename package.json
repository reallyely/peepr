{
  "scripts": {
    "lint": "biome check --write",
    "dev": "pnpm -r --parallel --env-file=.env dev",
    "test": "pnpm -r --parallel test",
    "build": "pnpm -r --parallel build",
    "start": "pnpm -r --parallel start",
    "deploy": "fly deploy"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@flydotio/dockerfile": "^0.7.8"
  }
}
